categories:
- app
questions:
- variable: defaultImage
  default: "true"
  description: "Use default Docker image"
  label: Use Default Image
  type: boolean
  show_subquestion_if: false
  group: "Container Images"
  subquestions:
  - variable: image.repository
    default: "10.10.15.32:5443/x86/drone"
    description: "Docker image name"
    type: string
    label: Image Name
  - variable: image.tag
    default: "1.9.0"
    description: "Docker image tag"
    type: string
    label: Image Tag
- variable: gitlabserver
  type: string
  description: "DRONE_GITLAB_SERVER"
  required: true
  lable: gitlab server
  group: gitlab
- variable: gitlabcliid
  type: string
  description: "DRONE_GITLAB_CLIENT_ID"
  required: true
  lable: gitlab client id
  group: gitlab
- variable: gitlabclicrt
  type: string
  description: "DRONE_GITLAB_CLIENT_SECRET"
  required: true
  lable: gitlab client secret
  group: gitlab
- variable: gitlabrpccrt
  type: string
  description: "DRONE_RPC_SECRET"
  required: true
  lable: gitlib rpc secret
  group: gitlab
- variable: gitlabserverhost
  type: string
  description: "DRONE_SERVER_HOST"
  required: true
  lable: gitlib server host
  group: gitlab
- variable: gitlabserverproto
  type: string
  description: "DRONE_SERVER_PROTO"
  required: true
  lable: gitlib server proto
  group: gitlab